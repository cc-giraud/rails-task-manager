<h1><%= @task.title %></h1>
<%= link_to "edit", edit_task_path(@task.id) %>
<%= link_to "Delete #{@task.title}",
            task_path(@task),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
